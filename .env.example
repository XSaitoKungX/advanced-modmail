# This file intentionally contains placeholders only.
# Loaded via @dotenvx/dotenvx and validated by src/config/env/ - see schema.ts for the contract.

DISCORD_TOKEN=
DISCORD_CLIENT_ID=
# Optional until the persistence layer consumes it.
DATABASE_URL=
# One of: fatal | error | warn | info | debug | trace
LOG_LEVEL=info
